// What is a stack? Is just a data structure which facilitates pushing on top and deleting form top. The pushing and popping should be of O(1) time.
// What is queue? Same as stack just he popping is done from the first position.
//
//How to Implement a stack using queues.  Use 2 queues , push the element in one empty queue (q1) then push all prev elements from second queue (q2) to the first one and then swap them. PUSH-O(N) - POP - pop from q1 -O(1).
//

